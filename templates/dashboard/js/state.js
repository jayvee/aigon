/* dashboard-esm-processed */
/** Compatibility shim — dashboard state lives in store.js (F624). */
export {
  POLL_MS,
  TS_MS,
  lsKey,
  state,
  isRepoHidden,
  toggleRepoVisibility,
} from './store.js';
