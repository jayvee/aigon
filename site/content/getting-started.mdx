---
title: Getting Started
description: Install Aigon and run your first spec-driven feature loop.
---

import { Callout, Tabs } from 'nextra/components'

## Prerequisites

You need **Node.js 18+**. That's it.

<Tabs items={['macOS', 'Ubuntu / Debian', 'Fedora', 'Arch']}>
  <Tabs.Tab>
    ```bash
    brew install node
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    curl -fsSL https://deb.nodesource.com/setup_22.x | sudo bash - && sudo apt-get install -y nodejs
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    sudo dnf install -y nodejs npm
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    sudo pacman -S --noconfirm nodejs npm
    ```
  </Tabs.Tab>
</Tabs>

The setup wizard installs everything else it needs (`tmux`, `lsof`, `gh`) once Aigon is on your PATH.

## Install Aigon

<Tabs items={['macOS', 'Linux']}>
  <Tabs.Tab>
    ```bash
    npm install -g @senlabsai/aigon@next
    aigon setup
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    sudo npm install -g @senlabsai/aigon@next
    aigon setup
    ```
  </Tabs.Tab>
</Tabs>

The wizard walks you through agent install, terminal preference, and dashboard start — see [Setup wizard](/docs/guides/setup-wizard) for the eight steps.

## Add Aigon to your project

```bash
cd /path/to/your/project
aigon apply
aigon install-agent cc gg          # Claude Code + Gemini
```

Agent codes — install whichever you have access to: `cc` Claude Code · `gg` Gemini CLI · `cx` Codex CLI · `cu` Cursor · `op` OpenCode · `km` Kimi Code CLI. Inside any of them, invoke Aigon commands as `/aigon:feature-do 07` (or `aigon-feature-do` for Codex).

## Install Aigon Pro (optional)

[Beta access](https://senlabs.ai) only. Pro adds autonomous mode, agent failover, scheduled & recurring features, and encrypted spec backups.

```bash
npm install -g @senlabsai/aigon-pro
aigon pro activate <your-key>
aigon server restart
```

See [Installing Aigon Pro](/docs/guides/pro-installation) for the full guide.

## Start using Aigon

Go to **[http://localhost:4100](http://localhost:4100)** — the wizard started the server during install. The Pipeline view shows your features across kanban lanes.

> **New to Aigon?** Walk through the [Brewboard tutorial](/docs/guides/brewboard-tutorial) first — it's a small Next.js seed repo with six pre-written feature specs ready to run. The tutorial gets you cloning, applying Aigon, and watching the dashboard drive a real feature in ~10 minutes. The wizard may already have cloned it for you; the tutorial covers both cases.

## Your first feature

### Via the dashboard (recommended)

{/* Screenshot: dashboard kanban with a card mid-flight */}
{/* <img src="/img/first-feature-dashboard.png" alt="Dashboard with a feature in progress" /> */}

1. **New feature** — click **New feature**, fill in title + description, save. Lands in **Inbox**.
2. **Prioritise** — click **Prioritise** on the card. Gets an ID, moves to **Backlog**.
3. **Start** — pick an agent and model in the modal. Aigon creates a worktree, launches the agent in a tmux session, moves to **In Progress**. Click the card to watch live.
4. **Code review** (optional) — click **Code Review** for a pass by a *different* agent. Reviewer diversity catches blind spots a same-model reviewer misses.
5. **Close** — runs gitleaks + semgrep gates, merges to main, runs tests, moves to **Done**.

### Via an agent CLI

Inside Claude Code (or any installed agent), the fastest path is `/aigon:feature-now`:

```
/aigon:feature-now dark-mode
Add a dark mode toggle to the top-right header.
Default to system preference. Store choice in localStorage.
```

The agent creates a spec, assigns an ID, sets up the workspace, and starts implementing — all in one conversation.

For more control, use individual commands: `feature-create` → `feature-prioritise` → `feature-start` → `feature-do` → `feature-code-review` → `feature-close`. 

## Updating Aigon

```bash
npm update -g @senlabsai/aigon@next
aigon apply --all
```

Re-applies templates across every registered repo. See [Applying Aigon updates](/docs/guides/applying-aigon-updates) for finer-grained control.

## What's next

- **[Drive Mode](/docs/guides/drive-mode)** — hands-on implementation with one agent
- **[Fleet Mode](/docs/guides/fleet-mode)** — multiple agents competing in parallel against the same spec
- **[Autonomous Mode](/docs/guides/autonomous-mode)** — hands-off long-running loops
- **[Research Workflow](/docs/guides/research-workflow)** — answer open questions before turning them into features
- **[Feedback Workflow](/docs/guides/feedback-workflow)** — triage user feedback into prioritised work
- **[Execution Modes](/docs/concepts/execution-modes)** — Drive, Fleet, Autopilot, and Swarm

<Callout type="warning">
**Beta software.** Aigon is early access — APIs and config formats may change between releases. File bugs at [github.com/jayvee/aigon/issues](https://github.com/jayvee/aigon/issues).
</Callout>
