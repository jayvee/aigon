---
title: "research-create"
description: "Create research name - creates topic in inbox"
---

## Synopsis

```bash
aigon research-create <topic-name>
```

## Shortcuts

- `/arc` (slash command) · `aigon arc` (CLI)

## Description

Create research \<name\> - creates topic in inbox

## Usage

```bash
# Slash command (Claude Code / Gemini)
/aigon:research-create <topic-name>

# CLI
aigon research-create <topic-name>
```

## Spec frontmatter

The inbox topic is created from the research template with YAML frontmatter including **`complexity`** and optional **`recommended_models`**, same idea as features. Those fields feed model/effort defaults when you **research-start** from the dashboard or CLI. See [Agents: Spec complexity and model defaults](/docs/reference/agents#spec-complexity-and-model-defaults).
