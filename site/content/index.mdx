---
title: Aigon Documentation
description: Aigon orchestrates Claude Code, Codex, and Cursor CLI (and more) to implement your specs in parallel, judged by an LLM — better code quality, no quota walls.
---

## One spec, multiple agents

> **Aigon orchestrates Claude Code, Codex, Cursor CLI (and more) to implement your specs in parallel, judged by an LLM, or through pre-defined review stages — resulting in improved code quality.**
>
> **Schedule your specs to run when you have quota (the dreaded 5hr window), or when you are asleep, avoiding FONUT — Fear Of Not Using Tokens.**

Aigon is an open-source, spec-driven harness for multi-agent coding. Each feature is a Markdown spec in your repo. Each agent runs in its own git worktree under tmux. You merge the diff that survives review. No SaaS account, no token resale, no lock-in.

![Aigon Dashboard — Reports Charts](/img/aigon-dashboard-reports-charts.png)

## What you get

**Catch what one model misses.** Different model families catch different classes of bug. When Claude reviews Claude, the reviewer shares the implementer's blind spots. Aigon Fleet runs Claude Code, Codex, Cursor CLI (and more) in parallel against the same spec, with a different coding agent as the merge reviewer. Reviewer diversity is the wedge.

**Stop hitting quota walls.** Every Pro-plan user hits 5-hour quota limits. Single-agent tools leave you stuck — wait, switch laptops, or pay overage. Aigon polls your Claude Code / Codex / Gemini CLI / Kimi quotas every 30 minutes, shows headroom on the dashboard, and auto-fails over to whichever coding agent still has quota. Three subscriptions, one elastic pool.

**Use the subscriptions you already own.** Aigon never sits in your billing path. Spend the Claude Pro, ChatGPT Plus, and Cursor Pro subscriptions you already have — Aigon orchestrates the agent CLIs, not their underlying model APIs. No tokens, no markup, no metering.

**Watch the work happen, locally.** Each agent runs in its own git worktree under tmux — `tmux attach` to any session and watch the work happen live. Specs live in your repo as Markdown; every workflow move is a git diff. No cloud sandbox, no opaque execution.

## How you drive it

Three surfaces, one state. **Slash commands** inside your existing CLIs (Claude Code, Gemini CLI, Codex, Cursor). A **local web dashboard** for the Kanban view. The **`aigon` CLI** for everything else. Every surface reads and writes the same specs and workflow state in your repo. Lifecycle: inbox → backlog → in-progress → review → done.

## Sections

<div className="mt-6 grid grid-cols-1 sm:grid-cols-2 gap-4">
  <a href="/docs/getting-started" className="block rounded-lg border border-gray-800 p-5 hover:border-gray-600 transition-colors no-underline">
    <h3 className="text-lg font-semibold mb-1">Getting Started</h3>
    <p className="text-sm text-gray-400 m-0">Install Aigon and run your first feature loop in under five minutes.</p>
  </a>
  <a href="/docs/concepts" className="block rounded-lg border border-gray-800 p-5 hover:border-gray-600 transition-colors no-underline">
    <h3 className="text-lg font-semibold mb-1">Concepts</h3>
    <p className="text-sm text-gray-400 m-0">Three workflows, four execution modes, and how Aigon organises your work.</p>
  </a>
  <a href="/docs/guides" className="block rounded-lg border border-gray-800 p-5 hover:border-gray-600 transition-colors no-underline">
    <h3 className="text-lg font-semibold mb-1">Guides</h3>
    <p className="text-sm text-gray-400 m-0">Step-by-step walkthroughs for Drive, Fleet, Research, and Dashboard.</p>
  </a>
  <a href="/docs/compare" className="block rounded-lg border border-gray-800 p-5 hover:border-gray-600 transition-colors no-underline">
    <h3 className="text-lg font-semibold mb-1">Compare</h3>
    <p className="text-sm text-gray-400 m-0">How Aigon differs from single-agent tools, intra-vendor harnesses, and cloud agents.</p>
  </a>
  <a href="/docs/reference" className="block rounded-lg border border-gray-800 p-5 hover:border-gray-600 transition-colors no-underline">
    <h3 className="text-lg font-semibold mb-1">Reference</h3>
    <p className="text-sm text-gray-400 m-0">CLI commands, configuration, agents, and file structure.</p>
  </a>
</div>
