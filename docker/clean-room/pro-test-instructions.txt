AIGON PRO - DOCKER TEST INSTRUCTIONS
=====================================
Run all commands from ~/src/aigon


STEP 1 - Build tarballs (if not already done)
----------------------------------------------
cd ~/src/aigon && npm pack

cd ~/src/aigon-pro && npm pack


STEP 2 - Build the Docker image
--------------------------------
cd ~/src/aigon

docker build -t aigon-clean-room docker/clean-room/


STEP 3 - Launch the container
------------------------------
docker run --rm -it -v ~/src/aigon:/home/dev/src/aigon -v ~/src/aigon-pro:/home/dev/src/aigon-pro -p 4102:4100 --hostname clean-room aigon-clean-room bash


STEP 4 - Inside container: automated check
-------------------------------------------
(Substitute your real Pro beta key for <your-pro-key> — never commit the real value)
AIGON_PRO_KEY=<your-pro-key> bash ~/src/aigon/docker/clean-room/smoke-test.sh --scenario 3


STEP 5 - Inside container: manual checks
-----------------------------------------
aigon pro status

aigon insights | head -3

aigon pro activate wrongkey

aigon insights 2>&1 | head -3

aigon pro activate <your-pro-key>


EXPECTED RESULTS
----------------
Step 4: All checks PASS, no FAIL lines
Step 5 aigon pro status:     Package @senlabsai/aigon-pro: installed  Pro key: present  Pro is active
Step 5 aigon insights:       Should return data (AADE Insights...)
Step 5 wrong key insights:   Should show: is a Pro feature. Install: ... Activate: ...
