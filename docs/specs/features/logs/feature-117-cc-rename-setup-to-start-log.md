---
commit_count: 3
lines_added: 269
lines_removed: 235
lines_changed: 504
files_touched: 52
fix_commit_count: 0
fix_commit_ratio: 0
rework_thrashing: false
rework_fix_cascade: false
rework_scope_creep: true
---

# Implementation Log: Feature 117 - rename-setup-to-start
Agent: cc

## Plan

Systematic rename following the spec's suggested order:
1. State machine (smallest blast radius)
2. Command handlers (feature.js, research.js)
3. Dashboard (actions.js, pipeline.js, logs.js)
4. Templates (file renames + content updates)
5. Agent configs + COMMAND_REGISTRY
6. Tests and documentation

## Progress

- Renamed all transition actions in state-machine.js (FEATURE_TRANSITIONS, RESEARCH_TRANSITIONS, FEATURE_ACTIONS, TRANSITION_DEFS)
- Renamed command handlers: `feature-setup` → `feature-start` in feature.js, `research-setup` → `research-start` in research.js
- Updated all error messages and usage strings across feature.js, research.js, misc.js, infra.js
- Updated feature-autopilot and research-autopilot internal calls to use new names
- Simplified dashboard dispatch: removed `requestFeatureOpen` chaining since feature-start handles terminal opening
- Renamed template files and updated content in ~20 template files
- Updated COMMAND_REGISTRY with new shortcuts
- Updated all test files (unit tests, e2e tests, dashboard e2e tests)
- Updated documentation (README, GUIDE, agent docs, marketing)
- 51 files changed, all syntax checks pass, no new test failures (5 pre-existing)

## Decisions

### Shortcut Reassignment
Followed spec recommendation: `afs` → feature-start, `afsb` → feature-submit.
For research: `ars` → research-start (took over from research-synthesize), `arsy` → research-synthesize (new shortcut).

### Dashboard Simplification
Removed `requestFeatureOpen` chaining from the `feature-start`/`research-start` dispatch cases in actions.js and pipeline.js. The CLI command now handles opening terminals after workspace creation, so the dashboard doesn't need to chain separate open requests.

### Historical Content Preserved
CHANGELOG entries and done/paused feature specs were NOT updated — they are historical records that should reference the command names as they existed at the time.

### Generated Working Copies Skipped
.claude/commands/ and .cursor/commands/ working copies were not updated — they are gitignored and will be regenerated by `aigon install-agent`.
