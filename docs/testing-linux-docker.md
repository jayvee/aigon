# Testing Aigon on Linux via Docker

This doc has moved to [`docker/clean-room/README.md`](../docker/clean-room/README.md).

That is the single setup for end-to-end installation testing. It covers:

- **Manual mode** — interactive shell, walk through install as a new user
- **Automated mode** — non-interactive smoke tests (`run.sh --auto --all`)
